{
    "buildCommand": "cd frontend && npm install && npm run build",
    "outputDirectory": "frontend/dist/entry-AI",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }